input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

filter {
  # Add processing if needed
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "medicine-price-tracker-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
